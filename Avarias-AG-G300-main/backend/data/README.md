# Data Folder

Place the Excel file here: `Drive atualizado.xlsx`

This file is excluded from git by default (sensitive warehouse data).
Upload it manually to Render via the dashboard or environment variable.
